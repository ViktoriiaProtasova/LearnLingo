import React from 'react';

const TeacherCard = ({
  name,
  surname,
  languages,
  levels,
  rating,
  reviews,
  price_per_hour,
  lessons_done,
  avatar_url,
  lesson_info,
  conditions,
  experience,
  isOnline,
  children,
}) => {
  return <div></div>;
};

export default TeacherCard;
