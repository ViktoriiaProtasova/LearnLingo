import styled from 'styled-components';

export const StyledLogin = styled.a`
  display: inline-flex;
  align-items: center;
  gap: 8px;

  .login-icon {
    width: 20px;
    height: 20px;
    color: var(--primary-yellow);
  }

  .login-text {
    font-size: 16px;
    font-weight: 700;
    line-height: 1.25;
  }
`;
